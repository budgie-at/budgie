name: Build and Publish to Stores

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    name: iOS build and publish
    permissions:
      contents: read
    uses: rnw-community/mobile-ci/.github/workflows/native-publish.yml@v1.10.0 # v1.10.0
    with:
      enable-ios: true
      enable-android: false
      ios-runner-labels: '["self-hosted","macOS","ARM64","macos-tartelet"]'
      app-dir: packages/app
      build-profile: production
      submit-profile: production
      asc-key-path: asc_api_key.p8
      install-command: pnpm install --frozen-lockfile
      publish-env: |
        EXPO_NO_TELEMETRY=1
        APP_VARIANT=production
        EXPO_APPLE_TEAM_TYPE=INDIVIDUAL
        EXPO_APPLE_TEAM_ID=3R8589YV24
        EXPO_USE_PRECOMPILED_MODULES=1
        RCT_USE_PREBUILT_RNCORE=1
        RCT_USE_RN_DEP=1
    secrets:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      ASC_API_KEY: ${{ secrets.EXPO_IOS_ASC_API_KEY }}
      ASC_KEY_ID: ${{ secrets.EXPO_IOS_ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.EXPO_IOS_ASC_ISSUER_ID }}
