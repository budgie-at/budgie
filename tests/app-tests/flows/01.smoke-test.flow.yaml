appId: ${APP_ID}
---
- startRecording:
    path: '01.smoke-test'
    optional: true

- launchApp:
    clearState: true

- extendedWaitUntil:
    visible: 'Total Balance'
    timeout: 30000

- assertVisible: 'Total Balance'
