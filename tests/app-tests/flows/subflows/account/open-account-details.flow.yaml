appId: ${APP_ID}
---

- scrollUntilVisible:
    element:
      id: 'AccountCard.${ACCOUNT_NAME}'
    direction: DOWN
- waitForAnimationToEnd

- tapOn:
    id: 'AccountCard.${ACCOUNT_NAME}'
- extendedWaitUntil:
    visible:
      id: 'AccountDetails.EditButton'
    timeout: 10000
