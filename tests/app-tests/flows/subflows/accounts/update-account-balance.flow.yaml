appId: ${APP_ID}
---

- tapOn:
    id: 'CreateAccount.Amount'
- eraseText: 100
- inputText: ${NEW_BALANCE}

- hideKeyboard
- tapOn:
    id: 'AccountForm.SubmitButton'

- extendedWaitUntil:
    visible:
      id: 'AccountCard.${ACCOUNT_NAME}'
    timeout: 3000
