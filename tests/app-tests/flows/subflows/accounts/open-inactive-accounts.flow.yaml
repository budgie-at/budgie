appId: ${APP_ID}
---

- openLink: 'budgie://settings/inactive'

- runFlow:
      when:
          visible: 'Open'
      commands:
          - tapOn: 'Open'

- extendedWaitUntil:
      visible:
          id: 'InactiveAccountsPage.Container'
      timeout: 15000
