appId: ${APP_ID}
---

- scrollUntilVisible:
    element:
      id: 'AccountCard.${ACCOUNT_NAME}'
    direction: DOWN
    visibilityPercentage: 40

- tapOn:
    id: 'AccountCard.${ACCOUNT_NAME}'
- extendedWaitUntil:
    visible:
      id: 'AccountDetails.Page'
    timeout: 10000
