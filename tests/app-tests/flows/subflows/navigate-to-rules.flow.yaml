appId: ${APP_ID}
---

- openLink: 'budgie://settings/rules'

- runFlow:
    when:
      visible: 'Open'
    commands:
      - tapOn: 'Open'

- extendedWaitUntil:
    visible:
      id: 'RulesPage.Page'
    timeout: 10000
