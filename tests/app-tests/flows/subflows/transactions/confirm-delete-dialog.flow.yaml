appId: ${APP_ID}
---
- extendedWaitUntil:
    visible: 'Are you sure?'
    timeout: 10000
- waitForAnimationToEnd
- retry:
    maxRetries: 3
    commands:
      - tapOn:
          point: '274,488'
      - extendedWaitUntil:
          notVisible: 'Are you sure?'
          timeout: 5000
