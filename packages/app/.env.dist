# AI Configuration
# Set to 'true' to disable AI features (LLM and Speech-to-Text)
# This prevents AI models from loading and crashing on fast refresh
# Default: undefined (AI features enabled)
EXPO_PUBLIC_AI_DISABLE=
