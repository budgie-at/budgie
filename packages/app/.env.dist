# AI Configuration
# Set to 'true' to disable AI features (LLM and Speech-to-Text)
# This prevents react-native-executorch from loading and crashing on fast refresh
# Default: undefined (AI features enabled)
AI_DISABLE=
