export enum WaitlistMessageKeyEnum {
    // Lowercase values preserve the existing serialized server-action contract.
    ALREADY_REGISTERED = 'already_registered',
    ERROR = 'error',
    INVALID_EMAIL = 'invalid_email',
    SUCCESS = 'success'
}
